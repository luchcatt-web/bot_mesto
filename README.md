# 💈 Telegram бот для барбершопа + YClients

Бот с полной интеграцией YClients:
- 📱 Собирает контакты клиентов через QR-код
- 📅 Получает записи из YClients  
- 🔔 Автоматически отправляет напоминания в Telegram

**Решает проблему:** когда у клиента скрыт номер телефона в Telegram — бот всё равно отправит напоминание!

## 🚀 Настройка

### 1. Создайте Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Введите название (например: "Барбершоп Напоминания")
4. Введите username (например: `mybarber_reminder_bot`)
5. **Скопируйте токен**

### 2. Получите токены YClients

#### Партнёрский токен (Partner Token)
1. Зайдите в [YClients Developer](https://developers.yclients.com/)
2. Зарегистрируйтесь как партнёр
3. Создайте приложение → получите Partner Token

#### Пользовательский токен (User Token)
1. В личном кабинете YClients → Настройки → API
2. Или через API авторизации:
```bash
curl -X POST "https://api.yclients.com/api/v1/auth" \
  -H "Authorization: Bearer YOUR_PARTNER_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"login": "ваш_email", "password": "ваш_пароль"}'
```

#### ID компании (Company ID)
1. Откройте YClients → ваш филиал
2. ID виден в URL: `https://yclients.com/company/XXXXXX` ← это Company ID
3. Или через API: `GET /api/v1/companies`

### 3. Настройте бота

Откройте `bot.py` и заполните:

```python
# Telegram
BOT_TOKEN = "123456:ABC-DEF..."

# YClients
YCLIENTS_PARTNER_TOKEN = "ваш_партнерский_токен"
YCLIENTS_USER_TOKEN = "ваш_пользовательский_токен"
YCLIENTS_COMPANY_ID = "123456"

# Барбершоп
BARBERSHOP_NAME = "Название"
BARBERSHOP_ADDRESS = "Адрес"
BARBERSHOP_PHONE = "+7 (999) 123-45-67"
```

### 4. Запустите

```bash
cd barbershop_bot
pip install -r requirements.txt
python bot.py
```

## 📱 Как это работает

```
┌─────────────────┐     ┌──────────────┐     ┌─────────────┐
│  Клиент         │     │  Ваш бот     │     │  YClients   │
│  сканирует QR   │────▶│  сохраняет   │◀────│  записи     │
│  делится номером│     │  телефон+ID  │     │             │
└─────────────────┘     └──────┬───────┘     └─────────────┘
                               │
                               ▼
                    ┌──────────────────┐
                    │ За 24ч и за 2ч   │
                    │ → напоминание    │
                    │ в Telegram       │
                    └──────────────────┘
```

1. **Клиент сканирует QR** → попадает в бота
2. **Нажимает кнопку** → делится номером телефона
3. **Бот сохраняет связку:** телефон ↔ Telegram ID
4. **Каждые 10 минут** бот проверяет записи в YClients
5. **Находит совпадение** по номеру телефона
6. **Отправляет напоминание** напрямую в Telegram!

## 📲 QR-код

Создайте QR-код со ссылкой `https://t.me/username_бота`:

```bash
pip install qrcode[pil]
python generate_qr.py
```

Распечатайте и повесьте на ресепшене! 🖨️

## 🔔 Напоминания

По умолчанию отправляются:
- За **24 часа** до визита
- За **2 часа** до визита

Изменить в `bot.py`:
```python
REMINDER_HOURS = [24, 2]  # Можно добавить: [48, 24, 2]
```

## 📊 Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Начать, поделиться номером |
| `/myrecords` | Посмотреть свои записи |

## 🖥 Запуск на сервере 24/7

### Вариант 1: systemd (Linux VPS)

```bash
sudo nano /etc/systemd/system/barbershop-bot.service
```

```ini
[Unit]
Description=Barbershop Telegram Bot
After=network.target

[Service]
User=www-data
WorkingDirectory=/path/to/barbershop_bot
ExecStart=/usr/bin/python3 bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl enable barbershop-bot
sudo systemctl start barbershop-bot
```

### Вариант 2: Docker

```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["python", "bot.py"]
```

```bash
docker build -t barbershop-bot .
docker run -d --restart=always barbershop-bot
```

## ❓ FAQ

**Q: BeautyBot всё ещё нужен?**
A: Нет! Этот бот заменяет его функцию напоминаний для зарегистрированных клиентов.

**Q: Что если клиент не сканировал QR?**
A: Для него напоминания пойдут через BeautyBot (если номер не скрыт) или не придут вообще.

**Q: Данные безопасны?**
A: Хранятся локально в `clients.db`. Делайте бэкапы!

**Q: Бот отправляет спам?**
A: Нет, только напоминания о записи тем, кто сам поделился номером.
